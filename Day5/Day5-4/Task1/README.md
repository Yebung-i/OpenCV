- 스트레칭 : 이미지에서 가장 어두운 밝기 값과 가장 밝은 밝기 값을 찾아내어, 가장 낮은 밝기 값을 0으로, 가장 높은 밝기 값은 255로 고정하고 그 사이의 값들을 비율대로 넒게 펼친다.
- 평활화 : 이미지 전체의 픽셀 밝기 분포를 조사하여, 누적 분포 함수를 계산한다. 그 후 픽셀들이 많이 뭉쳐 있는 어두운 대역이나 밝은 대역을 강제로 넒게 쪼개어 전체 이미지에서 모든 밝기 단계가 최대한 균일하게 나타나도록 한다.
